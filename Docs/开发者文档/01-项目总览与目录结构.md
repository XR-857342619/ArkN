# 01 项目总览与目录结构

> 文档同步版本：**v1.6.5**（2026-08-28）。目录与数量统计均以当前仓库为准。

## 1. 项目背景

- 本项目是 ArknightR（独钓寒江翎 开发的明日方舟同人模拟器）的 **N 版分支**。
- 原项目停止开发后，糖浆继续维护两年并于 2025/1 停止；本分支由 **X2r(None)** 与 **HJsama** 继续开发。
- 玩法：还原《明日方舟》的核心战斗（部署干员、费用回复、阻挡、技能、状态、弹道），并额外支持**玩家地图编辑器（地编）**、**DIY 单位编辑**与部分肉鸽（Dungeon）玩法。
- 当前版本：Git HEAD 为 `v1.6.5`（`062f1e31`）；`ProjectSettings.asset` 的 `bundleVersion` 仍为 `v1.5.14_Debug`，打包前需注意同步。
- 仓库根目录见 `README.md`。

## 2. 技术栈

| 项目 | 版本/来源 | 说明 |
|---|---|---|
| Unity | 2021.2.10f1 | `ProjectSettings/ProjectVersion.txt` |
| 脚本 | C# | 无 asmdef，全部在 Assembly-CSharp 中编译 |
| UI | FairyGUI（Assets/Plugins/FairyGUI） | UI 包从 `Assets/Bundles/UI/*_fui` 经 Addressables 加载 |
| 骨骼动画 | Spine Runtime 3.8（Assets/Plugins/Spine） | 单位/敌人/召唤物表现 |
| 寻路 | 自写 A*（`Assets/Scripts/Core/Map/PathFinder_AStar.cs`） | 敌人寻路由 `EnemyPathfinder` 委托调用；AstarPathfindingProject 保留但基本停用 |
| 资源加载 | Unity Addressables 1.19.15 | `Assets/AddressableAssetsData`，分组：Built In Data / Common / Default Local Group / StandPic |
| 配置读取 | ExcelDataReader + EPPlus（读/写 .xlsx） | `Assets/Plugins/ExcelDataReader`、`EPPlus.dll`；EPPlus 已按平台隔离 |
| 表达式 | System.Linq.Dynamic.Core + `System.Linq.Expressions` | `UnifiedExpressionEngine` 编译/缓存表达式 |
| 技能组合框架 | JsonSkill（自研） | `Core/Skill/Json/`、`Core/Skill/Effects/`，见 13/14 文档 |
| JSON | Newtonsoft.Json（Plugins 内置） | `JsonHelper` |
| Tween | DOTween | `Assets/Plugins/DOTween.dll` |
| 拼音 | Pinyin 插件 | 中文排序等 |
| 其他 DLL | SharpZipLib、System.Linq.Dynamic.Core 等 | 编辑工具/导表；Roslyn/System.Windows.Forms 已无运行时代码引用 |

## 3. 顶层目录

```
zhou-master/
├── Assets/
│   ├── AddressableAssetsData/  Addressables 配置（含 Windows/Android 构建产物目录）
│   ├── Bundles/                运行时资源（UI/Units/Effect/Data/Image/Audio...）
│   ├── Editor/                 编辑器脚本（导表、打包、Excel列同步、Spine 导入、FairyGUI）
│   ├── Plugins/                第三方插件（FairyGUI/Spine/Newtonsoft/Excel...）
│   ├── Res/                    源资源（Spine 骨骼、Effect 源文件、Map、Shader...）
│   ├── Scenes/                 场景（SampleScene/Default/MapBuilder/Maps/...）
│   ├── Scripts/                游戏代码（449 个 C# 文件，不含 .meta）
│   └── StreamingAssets/        首启后复制到热更目录的初始资源（被 .gitignore 忽略）
├── Excel/                      玩家 Excel 数据（Main/Test/一方通行/尊尼获加/Diy优胜）
│                               另含 `攻击范围编辑器.exe` 与本地存档 `data.sav`
├── Docs/                       项目文档
├── Docx/                       设计文档
├── Tools/                      批量处理 Spine 导出脚本（`批量处理spine导出.bat`）
├── UIProject/                  FairyGUI 工程源文件（Z 项目）
├── ProjectSettings/            Unity 工程设置
└── Packages/                   Unity 包清单
```

> `.gitignore` 忽略了 `Library/`、`Temp/`、`obj/`、`Logs/`、`Assets/StreamingAssets/` 等生成目录。克隆后需要用 Unity 打开重新生成 Library。

## 4. Assets/Scripts 代码目录

```
Assets/Scripts/
├── Init.cs / Database.cs / AudioManager.cs / Pool.cs / Singleton.cs / Test.cs
│   / SpineResourceManager.cs / StreamingAssetsCopyUtility.cs / ShowDebugInPhone.cs
├── Client/       存档与编队数据（GameData/Card/Team/Config/OpDamageInfo）
├── Config/       配置数据类（UnitData/SkillData/BuffData/...，实现 IConfig）
├── Core/         核心战斗逻辑（214 个 C# 文件）
│   ├── Battle.cs / BattleInput.cs / DamageInfo.cs / TriggerData.cs / CountDown.cs / SystemConfig.cs
│   ├── Unit/        单位基类 + Units.干员/敌人/普通单位/中立单位
│   ├── Skill/       技能基类 + JsonSkill 框架 + Skills.* 实现
│   │   ├── Types/            旧式技能子类（53 个，含 召唤相关/ 9 个）
│   │   ├── Json/             JSON 配置模型、校验、JsonConfigHelper、JsonSkill
│   │   ├── Effects/          效果器（Damage/Heal/Bullet/Summon 等 12 个 + 派发器）
│   │   └── TargetSelector/   主要目标选择器/排序器 + JsonSelectors
│   ├── Buff/        状态基类 + Buffs.* 实现（Types/ 递归 53 个，含 伤害重写类/数值变化/Sp）
│   ├── Bullet/      弹道基类 + Bullets.* 实现（Types/，8 个）
│   ├── Modify/      修饰器框架 + Modifys.* 实现（Types/，18 个）
│   ├── Map/         地图/Tile/A* 寻路
│   ├── View/        表现层（BattleManager/UnitModel/SpineModel/相机/拖尾）
│   ├── Effect/      特效逻辑（Effect/PullEffect）
│   └── Enum/        核心枚举
├── Dungeon/      肉鸽模式逻辑（Dungeon/Relic/Reward/View/...）
├── Helper/       通用工具（Json/Excel/Res/Save/Path/Log/表达式引擎/IconHelper/ExtextureLoader...）
├── MapBuild/     地编器逻辑（MapInfo/MapManager/MapGrid/ShowRange...）
├── Tool/         工具脚本（下载器/资源导出/骨骼查找...）
├── UI/           FairyGUI 界面
│   ├── AutoRelease/  FairyGUI 自动生成 partial 类（不要手改；含 BattleUI/MainUI/DIY/DungeonUI/MapBuilderUI/LandingUI）
│   ├── Main/  Battle/  Dungeon/  MapBuilder/  DIY/  Landing/  手写 partial 逻辑
│   ├── UIManager.cs / TipManager.cs / IGameUIView.cs
└── VFX/          高斯模糊、描边、特效管理（EffectManager）
```

> `UI/Healper/IconHelper.cs`、`UI/Healper/ExtextureLoader.cs` 已在 v1.6.3 移至 `Helper/`，引用路径以当前目录为准。

## 5. 资源目录（Assets/Bundles）

| 目录 | 内容 |
|---|---|
| `Bundles/UI/` | FairyGUI 包二进制（`*_fui`）与图集，36 个资源文件（10 个包/前缀） |
| `Bundles/Units/` | 单位预制体（2348 个资源文件，干员/敌人/召唤物） |
| `Bundles/Effect/` | 特效预制体（414 个资源文件） |
| `Bundles/Data/` | 导表产物 `*.txt`（每行一个 JSON）与地图数据 |
| `Bundles/Data/Map/<package>/` | 地图包（`*.txt` 地图 JSON，真机热更目录下为 `.map`；`config.ini`） |
| `Bundles/Image/` | 背景、立绘（StandPic）、头像（202 个资源文件） |
| `Bundles/Audio/` | 背景音乐（4 个资源文件） |
| `Bundles/Other/` | 地编用网格、线条、球等（68 个资源文件） |
| `Bundles/DungeonTile/` | 肉鸽地图块预制体（7 个资源文件） |

## 6. 场景

Build Settings 中的场景（`ProjectSettings/EditorBuildSettings.asset`）：

- `Assets/Scenes/SampleScene.unity` —— 主入口，挂载 `Init`、`UIManager` 等全局对象
- `Assets/Scenes/Default.unity`
- `Assets/Scenes/Maps/TestMap.unity`、`H6-4.unity`、`Rune_01-03.unity`、`Map1.unity`
- `Assets/Scenes/MapBuilder.unity` —— 地编器场景

其他开发/模板场景：`BoneFollowTest`、`Dungeon`、`PathTest`、`Tool`，以及 `Scenes/Maps/Rogue/` 下的 `Template`、`兽群`、`火力覆盖`、`路边劫匪`。

## 7. 关键第三方目录

- `Assets/Plugins/FairyGUI/Scripts/`：FairyGUI 运行时代码
- `Assets/Plugins/Spine/`：Spine 运行时与编辑器
- `Assets/Plugins/Newtonsoft.Json/`：JSON.NET
- `Assets/Plugins/ExcelDataReader/` + `EPPlus.dll` + `Excel.dll`：Excel 读写
- `Assets/Plugins/CurvedLine/`：曲线绘制（已转 UTF-8）
- `Assets/Plugins/Pinyin/`：中文拼音排序
